framework:
    messenger:
        transports:
            sync: 'sync://'
            async: '%env(MESSENGER_TRANSPORT_DSN)%'

        routing:
            App\Message\Notification\NotificationEventMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
